# Admin Dashboard UI 스펙

이 문서는 **관리자 대시보드** UI 스펙을 정의한다.

> **경로**: `/admin`
> **권한**: 관리자 전용
> **관련 API**: `/api/logs/stats`, `/api/orders`

---

## 1. 페이지 개요

관리자가 시스템 전반의 현황을 한눈에 파악할 수 있는 대시보드.

### 1.1 주요 기능
- 주문 통계 요약
- 최근 주문 목록
- 보안 로그 알림
- 빠른 액세스 메뉴

---

## 2. 레이아웃 구조

```tsx
<AdminDashboard>
  <PageHeader title="대시보드" />

  {/* 통계 카드 */}
  <StatsGrid>
    <StatCard title="오늘 주문" value={5} trend="+2" />
    <StatCard title="입금 대기" value={3} status="warning" />
    <StatCard title="발송 대기" value={2} status="info" />
    <StatCard title="이번 달 매출" value="₩850,000" trend="+15%" />
  </StatsGrid>

  {/* 최근 주문 */}
  <RecentOrders />

  {/* 빠른 액세스 */}
  <QuickActions />
</AdminDashboard>
```

---

## 3. 컴포넌트 상세

### 3.1 통계 카드 (StatCard)

**Props:**
```ts
interface StatCardProps {
  title: string;        // 통계 이름
  value: string | number; // 통계 값
  trend?: string;       // 증감률 (예: "+15%", "-5")
  status?: 'info' | 'warning' | 'success' | 'danger';
  icon?: ReactNode;
}
```

**UI:**
```
┌─────────────────────────┐
│ 📦 오늘 주문            │
│                         │
│ 5건                     │
│ ↑ +2 (전일 대비)        │
└─────────────────────────┘
```

**데이터 소스:**
- `/api/logs/stats?eventType=order_created&dateFrom={today}`
- `/api/orders?status=PENDING&dateFrom={today}`

### 3.2 최근 주문 목록 (RecentOrders)

**UI:**
```
최근 주문 (10건)                          [전체 보기 →]

┌──────────────────────────────────────────────────────┐
│ 주문번호         │ 고객명  │ 상품    │ 금액   │ 상태 │
├──────────────────────────────────────────────────────┤
│ ORD-20250115-001 │ 홍길동 │ 미루루... │ 10,000 │ 입금대기 │
│ ORD-20250115-002 │ 김철수 │ 키링    │ 5,000  │ 입금확인 │
└──────────────────────────────────────────────────────┘
```

**기능:**
- 클릭 시 주문 상세 페이지로 이동
- 상태별 색상 구분
- 최근 10건만 표시

**데이터 소스:**
- `GET /api/orders?page=1&limit=10`

### 3.3 빠른 액세스 (QuickActions)

**UI:**
```
빠른 액세스

┌─────────┐ ┌─────────┐ ┌─────────┐
│ + 상품   │ │ + 아티스트 │ │ + 프로젝트 │
└─────────┘ └─────────┘ └─────────┘
```

**액션:**
- 상품 등록 페이지로 이동: `/admin/products/new`
- 아티스트 등록 페이지로 이동: `/admin/artists/new`
- 프로젝트 등록 페이지로 이동: `/admin/projects/new`

---

## 4. 상태 관리

### 4.1 React Query Hooks

```ts
// 오늘 주문 통계
const { data: todayOrders } = useQuery({
  queryKey: ['admin', 'stats', 'today-orders'],
  queryFn: () => fetchTodayOrderStats(),
});

// 최근 주문
const { data: recentOrders } = useQuery({
  queryKey: ['admin', 'orders', 'recent'],
  queryFn: () => fetchRecentOrders({ limit: 10 }),
  refetchInterval: 30000, // 30초마다 자동 새로고침
});
```

---

## 5. 1차 MVP 범위

### 포함 ✅
- ✅ 통계 카드 (오늘 주문, 입금 대기, 발송 대기, 이번 달 매출)
- ✅ 최근 주문 10건
- ✅ 빠른 액세스 버튼

### 제외 ⏸️
- ⏸️ 매출 차트 (일별, 월별)
- ⏸️ 상품별 판매 통계
- ⏸️ 최근 로그인 사용자
- ⏸️ 시스템 알림

---

## 6. 예시 코드

```tsx
// app/admin/page.tsx
import { StatsGrid } from '@/components/admin/StatsGrid';
import { RecentOrders } from '@/components/admin/RecentOrders';
import { QuickActions } from '@/components/admin/QuickActions';

export default async function AdminDashboard() {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold">대시보드</h1>

      <StatsGrid />
      <RecentOrders />
      <QuickActions />
    </div>
  );
}
```

---

## 7. 참고 문서

- Admin 메인: `/specs/ui/admin/index.md`
- 주문 관리: `/specs/ui/admin/orders.md`
- Logs API: `/specs/api/server/routes/logs/index.md`
- Orders API: `/specs/api/server/routes/orders/index.md`
